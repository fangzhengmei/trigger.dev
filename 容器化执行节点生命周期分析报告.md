# 容器化执行节点生命周期分析报告

## 1. 系统架构概览

Trigger.dev 平台采用分层架构设计，将执行节点的生命周期管理分为多个核心模块：

```
┌─────────────────────────────────────────────────────────────────┐
│                     编排平台层 (apps/supervisor)                │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  DockerManager  │  │   K8sManager    │  │  ComputeManager │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  PodCleaner     │  │ FailedPodHandler│  │ ResourceMonitor │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ▲
                              │
┌─────────────────────────────────────────────────────────────────┐
│                  会话与队列层 (packages/core/v3)               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │SupervisorSession│  │ ConsumerPool    │  │  QueueConsumer  │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│  ┌─────────────────┐  ┌─────────────────┐                        │
│  │ HeartbeatsMgr   │  │ WorkloadServer  │                        │
│  └─────────────────┘  └─────────────────┘                        │
└─────────────────────────────────────────────────────────────────┘
                              ▲
                              │
┌─────────────────────────────────────────────────────────────────┐
│                    任务引擎层 (internal-packages)               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  DequeueSystem  │  │  RunLocker     │  │  RunQueue       │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │PendingVersionSys│  │ ExecutionSnapSys│  │ RunAttemptSystem│  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ▲
                              │
┌─────────────────────────────────────────────────────────────────┐
│                    消息队列层 (apps/webapp/v3)                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │      MarQS      │  │ VisibilityTimeout│  │  Worker API    │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 节点生命周期详解

### 2.1 启动注册流程

**核心流程：**

1. **Supervisor 初始化** (`apps/supervisor/src/index.ts:36-217`)
   - 根据环境变量自动检测运行环境（Kubernetes/Docker/Compute）
   - 创建对应 `WorkloadManager` 实例
   - 初始化 `SupervisorSession` 会话管理器

2. **节点连接注册** (`packages/core/src/v3/runEngineWorker/supervisor/session.ts:169-196`)
   ```typescript
   // 调用 connect API 注册节点
   const connect = await this.httpClient.connect({
     metadata: { workerVersion: VERSION },
   });
   ```
   - API 端点：`POST /engine/v1/worker-actions/connect`
   - 服务端处理：`apps/webapp/app/routes/engine.v1.worker-actions.connect.ts`
   - 认证通过 `WorkerToken` 和 `ManagedWorkerSecret` 双因素验证

3. **启动消费者池** (`packages/core/src/v3/runEngineWorker/supervisor/consumerPool.ts:135-164`)
   - 根据伸缩策略（`none`/动态）初始化消费者数量
   - 静态模式：`maxConsumerCount` 个消费者
   - 动态模式：`minConsumerCount` 个消费者起步，按需扩容

4. **启动心跳服务** (`packages/core/src/v3/runEngineWorker/supervisor/session.ts:64-80`)
   - 按 `heartbeatIntervalSeconds` 定期发送心跳
   - 携带 CPU/内存使用信息

**关键代码位置：**
- 会话启动：`session.ts:169-207`
- HTTP 客户端：`http.ts:60-73`
- 工作机组管理：`workerGroupService.server.ts:9-94`

---

### 2.2 任务领取机制

**核心流程：**

1. **消费者轮询** (`packages/core/src/v3/runEngineWorker/supervisor/queueConsumer.ts:64-150`)
   ```typescript
   // 轮询间隔策略
   - 有任务时：intervalMs (默认 1000ms)
   - 无任务时：idleIntervalMs (默认更长)
   ```

2. **预出队检查** (`queueConsumer.ts:79-111`)
   ```typescript
   // 资源检查钩子
   preDequeue?: PreDequeueFn; // 返回 maxResources 和 skipDequeue
   preSkip?: PreSkipFn;       // 跳过时执行
   ```
   - Docker 环境检查节点资源（CPU < 25% 或内存 < 25% 时跳过）

3. **调用 dequeue API** (`queueConsumer.ts:115-138`)
   - API 端点：`POST /engine/v1/worker-actions/dequeue`
   - 服务端处理：`apps/webapp/app/routes/engine.v1.worker-actions.dequeue.ts`

4. **服务端出队逻辑** (`internal-packages/run-engine/src/engine/systems/dequeueSystem.ts:105-200`)
   1. 从 `RunQueue` 公平出队（考虑多租户公平性）
   2. 应用 `RunLocker` 分布式锁防止并发修改
      ```typescript
      // runLock 基于 Redlock 实现
      const dequeuedRun = await this.$.runLock.lock(
        "dequeueFromWorkerQueue",
        [runId],
        async () => { /* ... */ }
      );
      ```
   3. 验证任务状态和版本匹配
   4. 锁定到特定版本（`lockedToVersion`）

5. **伸缩决策** (`consumerPool.ts:185-200`)
   - 基于 EWMA（指数加权移动平均）平滑队列长度
   - 支持多种伸缩策略（`none`/`linear`/`pid` 等）
   - 扩容/缩容冷却时间防止抖动

**关键代码位置：**
- 消费者轮询：`queueConsumer.ts:64-150`
- 分布式锁：`locking.ts:148-200`
- 伸缩策略：`scalingStrategies.ts`

---

### 2.3 心跳维护机制

**双层心跳架构：**

#### 2.3.1 Supervisor 节点心跳

**发送端** (`packages/core/src/v3/runEngineWorker/supervisor/session.ts:64-80`)
```typescript
// 定期发送节点心跳
this.heartbeat = new IntervalService({
  onInterval: async () => {
    const body = this.getHeartbeatBody(); // CPU/内存/任务信息
    await this.httpClient.heartbeatWorker(body);
  },
  intervalMs: opts.heartbeatIntervalSeconds * 1000,
});
```

**接收端** (`apps/webapp/app/routes/engine.v1.worker-actions.heartbeat.ts`)
- API 端点：`POST /engine/v1/worker-actions/heartbeat`
- 调用 `authenticatedWorker.heartbeatWorkerInstance()`

#### 2.3.2 任务执行心跳

**VisibilityTimeout 策略** (`apps/webapp/app/v3/marqs/v3VisibilityTimeout.server.ts`)
```typescript
// 心跳超时处理
export class V3GraphileVisibilityTimeout implements VisibilityTimeoutStrategy {
  async startHeartbeat(messageId: string, timeoutInMs: number) {
    // 安排超时任务
    await TaskRunHeartbeatFailedService.enqueue(
      messageId, 
      new Date(Date.now() + timeoutInMs)
    );
  }

  async heartbeat(messageId: string, timeoutInMs: number) {
    // 重新安排超时（重置倒计时）
    await TaskRunHeartbeatFailedService.enqueue(
      messageId, 
      new Date(Date.now() + timeoutInMs)
    );
  }

  async cancelHeartbeat(messageId: string) {
    // 任务完成，取消超时
    await TaskRunHeartbeatFailedService.dequeue(messageId);
  }
}
```

**心跳超时处理** (`apps/webapp/app/v3/taskRunHeartbeatFailed.server.ts:12-156`)
```typescript
switch (taskRun.status) {
  case "PENDING":
  case "DEQUEUED":
  case "WAITING_TO_RESUME":
  case "PAUSED":
    // 将任务重新入队 (nack)
    const backInQueue = await marqs?.nackMessage(taskRun.id);
    break;
    
  case "EXECUTING":
  case "RETRYING_AFTER_FAILURE":
    // 直接标记失败（执行中丢失心跳风险较高）
    await service.call(taskRun.friendlyId, {
      ok: false,
      error: {
        type: "INTERNAL_ERROR",
        code: TaskRunErrorCodes.TASK_RUN_HEARTBEAT_TIMEOUT,
        message: "Did not receive a heartbeat from the worker in time",
      },
    });
    break;
}
```

**关键代码位置：**
- 心跳管理器：`manager.ts:5-78`
- 超时策略：`v3VisibilityTimeout.server.ts:1-39`
- 心跳失败处理：`taskRunHeartbeatFailed.server.ts:12-156`

---

### 2.4 下线回收流程

**优雅下线流程：**

1. **信号捕获** (`packages/redis-worker/src/worker.ts:1155-1188`)
   ```typescript
   private setupShutdownHandlers() {
     shutdownManager.register(
       `redis-worker:${this.options.name}`, 
       this.shutdown.bind(this)
     );
   }
   ```

2. **停止消费者** (`consumerPool.ts:166-179`)
   ```typescript
   async stop() {
     this.isEnabled = false;
     // 停止所有消费者（不再领取新任务）
     Array.from(this.consumers.values()).forEach((consumer) => consumer.stop());
     this.consumers.clear();
   }
   ```

3. **等待任务完成** (`worker.ts:1159-1183`)
   ```typescript
   private async shutdown(signal?: NodeJS.Signals) {
     this.isShuttingDown = true;
     
     // 等待所有 worker 循环完成或超时
     await Promise.race([
       Promise.all(this.workerLoops),
       Worker.delay(this.shutdownTimeoutMs), // 默认 60s 超时
     ]);
     
     // 清理资源
     await this.subscriber?.unsubscribe();
     await this.subscriber?.quit();
     await this.queue.close();
   }
   ```

4. **Kubernetes Pod 清理** (`apps/supervisor/src/services/podCleaner.ts`)
   - 定期扫描已完成/失败的 Pod
   - 批量清理（`batchSize`）防止 API 限流
   - 按 `intervalMs` 间隔执行

5. **失败 Pod 处理** (`apps/supervisor/src/services/failedPodHandler.ts`)
   - 使用 Kubernetes Informer 监听 Pod 状态
   - 检测到 `status.phase=Failed` 的 Pod
   - 特殊退出码 `200` 表示优雅关闭，不做额外处理
   - 其他失败情况记录指标后删除 Pod

**关键代码位置：**
- Worker 下线：`worker.ts:1155-1188`
- Pod 清理：`podCleaner.ts`
- 失败 Pod 处理：`failedPodHandler.ts:18-326`

---

## 3. 编排平台差异抹平机制

### 3.1 多运行时支持

**WorkloadManager 抽象层** (`apps/supervisor/src/workloadManager/types.ts:16-18`)
```typescript
export interface WorkloadManager {
  create: (opts: WorkloadManagerCreateOptions) => Promise<unknown>;
}
```

**三种实现：**

| 运行时 | 实现类 | 关键特性 |
|--------|--------|----------|
| Docker | `DockerWorkloadManager` | 本地开发，快速启动，资源限制可选 |
| Kubernetes | `KubernetesWorkloadManager` | 生产环境，亲和性/容忍度，资源配额 |
| Compute | `ComputeWorkloadManager` | 外部计算网关，快照/恢复支持 |

**自动检测** (`apps/supervisor/src/index.ts:50-129`)
```typescript
private readonly isKubernetes = isKubernetesEnvironment(env.KUBERNETES_FORCE_ENABLED);

// 运行时选择
if (env.COMPUTE_GATEWAY_URL) {
  this.workloadManager = new ComputeWorkloadManager({...});
} else {
  this.workloadManager = this.isKubernetes
    ? new KubernetesWorkloadManager(workloadManagerOptions)
    : new DockerWorkloadManager(workloadManagerOptions);
}
```

### 3.2 环境变量标准化

**统一注入** (`docker.ts:69-111`, `kubernetes.ts:136-170`)

无论底层运行时如何，都注入统一的环境变量：
```typescript
const envVars: string[] = [
  `TRIGGER_DEQUEUED_AT_MS=${opts.dequeuedAt.getTime()}`,
  `TRIGGER_POD_SCHEDULED_AT_MS=${Date.now()}`,
  `TRIGGER_ENV_ID=${opts.envId}`,
  `TRIGGER_RUN_ID=${opts.runFriendlyId}`,
  `TRIGGER_SUPERVISOR_API_PROTOCOL=...`,
  `TRIGGER_SUPERVISOR_API_PORT=...`,
  `TRIGGER_SUPERVISOR_API_DOMAIN=...`,
  `TRIGGER_MACHINE_CPU=${opts.machine.cpu}`,
  `TRIGGER_MACHINE_MEMORY=${opts.machine.memory}`,
  // ... 更多标准化变量
];
```

### 3.3 资源监控抽象

**ResourceMonitor 接口** (`apps/supervisor/src/resourceMonitor.ts`)
```typescript
export interface ResourceMonitor {
  getNodeResources(): Promise<{
    cpuAvailable: number;
    memoryAvailable: number;
    // ...
  }>;
}

// 三种实现
- KubernetesResourceMonitor
- DockerResourceMonitor  
- NoopResourceMonitor
```

**关键代码位置：**
- 运行时抽象：`types.ts:16-18`
- 环境标准化：`docker.ts:69-111`
- 资源监控：`resourceMonitor.ts`

---

## 4. 节点掉线时任务接管机制

### 4.1 检测机制

**三层检测：**

1. **网络层检测** - Socket.IO 连接断开事件
2. **心跳层检测** - VisibilityTimeout 超时
3. **Kubernetes 层检测** - Pod 状态监听

### 4.2 任务重入队 (NACK)

**NACK 流程** (`internal-packages/run-engine/src/run-queue/index.ts`)
```typescript
// 当心跳超时或节点掉线时
async nackMessage(messageId: string): Promise<boolean> {
  // 1. 解锁任务
  await this.runLock.unlock([messageId]);
  
  // 2. 将消息从处理队列移回待处理队列
  // 3. 增加 nack 计数（防止无限重试）
  const nackCount = await this.incrementNackCounter(messageId);
  
  // 4. 超过最大 nack 次数则移至死信队列
  if (nackCount > this.maximumNackCount) {
    await this.moveToDeadLetterQueue(messageId);
    return false;
  }
  
  // 5. 重新入队
  await this.requeueMessage(messageId);
  return true;
}
```

### 4.3 状态机处理

**心跳失败时的状态转换** (`taskRunHeartbeatFailed.server.ts:51-155`)

| 当前状态 | 处理方式 | 原因 |
|---------|----------|------|
| `PENDING` / `DEQUEUED` / `WAITING_TO_RESUME` / `PAUSED` | NACK 重新入队 | 任务未开始执行，可安全重试 |
| `EXECUTING` / `RETRYING_AFTER_FAILURE` | 标记失败 | 执行中掉线可能已产生副作用，避免重复执行 |
| `DELAYED` / `PENDING_VERSION` / `WAITING_FOR_DEPLOY` | ACK 后重新调度 | 等待状态，由专门系统处理 |
| 终态（`COMPLETED_*`/`CANCELED` 等） | ACK 忽略 | 任务已完成，发送取消信号给残留容器 |

### 4.4 分布式锁保障

**RunLocker 机制** (`internal-packages/run-engine/src/engine/locking.ts:70-145`)
```typescript
// 基于 Redlock 的分布式锁
export class RunLocker {
  private redlock: InstanceType<typeof redlock.default>;
  
  async lock<T>(
    name: string,
    resources: string[],
    callback: (signal: redlock.RedlockAbortSignal) => Promise<T>,
    options?: LockRetryConfig
  ): Promise<T> {
    // 锁自动延期：接近过期时自动续期
    // 锁超时：默认 5000ms，防止死锁
  }
}
```

**关键代码位置：**
- NACK 逻辑：`run-queue/index.ts`
- 状态处理：`taskRunHeartbeatFailed.server.ts:51-155`
- 分布式锁：`locking.ts:70-145`

---

## 5. 版本滚动时防止双重消费机制

### 5.1 PENDING_VERSION 状态

**排队时的版本持有** (`internal-packages/run-engine/src/engine/systems/pendingVersionSystem.ts:19-141`)

```typescript
// 新任务入队时，如果没有匹配的 worker 版本
async enqueueRun(...) {
  if (!hasMatchingWorkerVersion) {
    // 设置为 PENDING_VERSION 状态，不进入执行队列
    await tx.taskRun.update({
      where: { id: run.id },
      data: { status: "PENDING_VERSION" },
    });
    return;
  }
  // ... 正常入队
}
```

**版本部署后触发** (`pendingVersionSystem.ts:135-141`)
```typescript
async scheduleResolvePendingVersionRuns(backgroundWorkerId: string): Promise<void> {
  // 当新版本部署完成后，后台处理 PENDING_VERSION 的任务
  await this.$.worker.enqueue({
    job: "queueRunsPendingVersion",
    payload: { backgroundWorkerId },
  });
}
```

**批量处理** (`pendingVersionSystem.ts:19-133`)
```typescript
async enqueueRunsForBackgroundWorker(backgroundWorkerId: string) {
  // 批量处理，防止一次处理过多
  const maxCount = this.options.queueRunsPendingVersionBatchSize ?? 200;
  
  const pendingRuns = await this.$.readOnlyPrisma.taskRun.findMany({
    where: { status: "PENDING_VERSION", /* 匹配新版本 */ },
    orderBy: { createdAt: "asc" },
    take: maxCount + 1, // 多取一条判断是否还有更多
  });
  
  for (const run of pendingRuns.slice(0, maxCount)) {
    await this.$.prisma.$transaction(async (tx) => {
      // 1. 更新状态为 PENDING
      const updatedRun = await tx.taskRun.update({
        where: { id: run.id },
        data: { status: "PENDING" },
      });
      // 2. 正常入队
      await this.enqueueSystem.enqueueRun({ run: updatedRun, ... });
    });
  }
  
  // 还有更多，继续调度
  if (pendingRuns.length > maxCount) {
    await this.scheduleResolvePendingVersionRuns(backgroundWorkerId);
  }
}
```

### 5.2 版本锁定机制

**出队时锁定版本** (`dequeueSystem.ts:164-200`)
```typescript
// 任务出队时，锁定到特定版本
const dequeuedRun = await this.$.runLock.lock(
  "dequeueFromWorkerQueue",
  [runId],
  async () => {
    // ... 验证版本匹配
    // lockedToVersion 字段确保任务只能被特定版本的 worker 处理
  }
);
```

### 5.3 并发计数器 TTL

**防止 v1/v2 版本重叠漂移** (`run-queue/index.ts:84-86`)
```typescript
/**
 * TTL (seconds) applied to the per-base-queue lengthCounter/runningCounter on
 * lazy-init. Bounds the maximum window for any drift accumulated during a
 * rolling-deploy v1/v2 overlap. INCR/DECR do NOT extend the TTL, so the
 * counter expires this long after init regardless of activity, and the next
 * CK operation re-anchors from ckIndex. Default: 86400 (24h).
 */
counterTtlSeconds?: number;
```

### 5.4 部署标识与去重

**部署友好 ID 验证** (`dequeueSystem.ts:44-86`)
```typescript
type RunWithBackgroundWorkerTasksResult =
  | { success: false; code: "NO_WORKER" | "TASK_NOT_IN_LATEST" | "BACKGROUND_WORKER_MISMATCH" | ... }
  | { success: true; run: ...; worker: ...; deployment: ... };

// 出队前验证：
// 1. 任务属于最新版本的 worker 吗？
// 2. 部署版本匹配吗？
// 3. 任务标识符在最新版本中存在吗？
```

**关键代码位置：**
- PENDING_VERSION 系统：`pendingVersionSystem.ts:19-141`
- 版本锁定：`dequeueSystem.ts:164-200`
- 并发计数器 TTL：`run-queue/index.ts:84-86`

---

## 10. 节点掉线任务接管端到端链路详解

### 10.1 触发条件与检测链路

#### 10.1.1 心跳丢失检测流程

**触发源：任务级心跳超时**

```
任务执行中
    ↓
任务心跳开始 (VisibilityTimeout.startHeartbeat)
    ↓
Graphile Worker 调度 TaskRunHeartbeatFailed 任务
    ├─ 正常路径：Worker 定期 heartbeat() → 重新调度 TaskRunHeartbeatFailed（重置倒计时）
    └─ 超时路径：到达超时时间 → 触发 TaskRunHeartbeatFailedService.call()
```

**关键代码路径** (`apps/webapp/app/v3/marqs/v3VisibilityTimeout.server.ts:1-39
```typescript
// 开始心跳时调度一个超时任务
async startHeartbeat(messageId: string, timeoutInMs: number) {
  await TaskRunHeartbeatFailedService.enqueue(
    messageId, 
    new Date(Date.now() + timeoutInMs)
  );
}

// 每次心跳重新调度（本质是删除旧任务，创建新任务
async heartbeat(messageId: string, timeoutInMs: number) {
  await TaskRunHeartbeatFailedService.dequeue(messageId);
  await TaskRunHeartbeatFailedService.enqueue(
    messageId, 
    new Date(Date.now() + timeoutInMs)
  );
}
```

#### 10.1.2 三层检测机制的协作

| 检测层级 | 触发时机 | 响应延迟 | 处理范围 |
|---------|----------|----------|----------|
| **任务级心跳 | 任务执行中每 X 秒 | 较短（如 30s） | 单个任务 |
| **节点级心跳 | 心跳 | 节点每 Y 秒 | 较长（如 60s） | 节点上所有任务 |
| **K8s Pod 监听 | Pod 状态变更 | 几乎实时 | Pod 级故障 |

---

### 10.2 状态机详细转换链路

#### 10.2.1 PENDING 状态处理

**当前状态: `PENDING`

**处理流程：** NACK 重新入队

**代码实现** (`internal-packages/run-engine/src/run-queue/index.ts:885-963
```typescript
public async nackMessage({
  orgId,
  messageId,
  retryAt,
  incrementAttemptCount = true,
  skipDequeueProcessing = false,
}: {
  orgId: string;
  messageId: string;
  retryAt?: number;
  incrementAttemptCount?: boolean;
  skipDequeueProcessing?: boolean;
}) {
  const maxAttempts = this.retryOptions.maxAttempts ?? defaultRetrySettings.maxAttempts;

  // 1. 读取消息
  const message = await this.readMessage(orgId, messageId);
  if (!message) return;

  // 2. 检查重试次数
  if (incrementAttemptCount) {
    message.attempt = message.attempt + 1;
    if (message.attempt >= maxAttempts) {
      await this.#callMoveToDeadLetterQueue({ message });
      return false;
    }
  }

  // 3. 调度 worker 队列处理任务（去重，500ms 延迟）
  if (!skipDequeueProcessing) {
    await this.worker.enqueueOnce({
      id: dedupQueueKey,
      job: "processQueueForWorkerQueue",
      payload: { queueKey, environmentId },
      availableAt: new Date(Date.now() + 500),
    });
  }

  // 4. 调用 Lua 脚本执行 NACK
  await this.#callNackMessage({ message, retryAt });

  return true;
}
```

**Lua 脚本原子操作** (`internal-packages/run-engine/src/run-queue/index.ts:4453-4493
```lua
-- 原子操作保证一致性
redis.call('SET', messageKey, messageData)          -- 更新消息数据
redis.call('SREM', queueCurrentConcurrencyKey, messageId)  -- 释放并发锁
redis.call('SREM', envCurrentConcurrencyKey, messageId)  -- 释放环境并发
redis.call('SREM', queueCurrentDequeuedKey, messageId)   -- 从出队集合移除
redis.call('SREM', envCurrentDequeuedKey, messageId)   -- 从环境出队集合移除
redis.call('ZADD', messageQueueKey, messageScore, messageId) -- 重新入队
redis.call('ZADD', envQueueKey, messageScore, messageId) -- 重新入环境队列
```

#### 10.2.2 DEQUEUED 状态处理

**当前状态: `DEQUEUED`

**处理流程：** NACK 重新入队（与 PENDING 相同逻辑相同

**关键区别：
- `DEQUEUED` 表示任务已被领取但尚未开始执行
- 此时任务还未产生任何副作用，可安全重试
- 重试次数 +1，但不增加数据库状态变更

#### 10.2.3 EXECUTING 状态处理

**当前状态: `EXECUTING` 或 `RETRYING_AFTER_FAILURE`

**处理流程：** 直接标记为失败（不重试）

**代码实现** (`apps/webapp/app/v3/taskRunHeartbeatFailed.server.ts:108-135
```typescript
case "EXECUTING":
case "RETRYING_AFTER_FAILURE":
  // 执行中丢失心跳，风险较高
  // 可能已经执行了部分逻辑，产生副作用
  // 避免重复执行
  await service.call(taskRun.friendlyId, {
    ok: false,
    error: {
      type: "INTERNAL_ERROR",
      code: TaskRunErrorCodes.TASK_RUN_HEARTBEAT_TIMEOUT,
      message: "Did not receive a heartbeat from the worker in time",
    },
  });
  break;
```

**设计考量：
- **幂等性风险**：执行中的任务可能已经产生了副作用（如发送了邮件、修改了数据库等）
- **避免双重消费风险**：如果重试，可能导致重复执行
- **用户可重试机制**：用户可以手动重试需要自行实现幂等性保护

#### 10.2.4 WAITING_TO_RESUME / PAUSED 状态处理

**当前状态: `WAITING_TO_RESUME` 或 `PAUSED`

**处理流程：** NACK 重新入队

**特殊说明：
- 这些状态表示任务在等待外部事件（如等待子任务完成、等待人工审核等）
- 节点掉线不影响任务状态本身，可安全重试

#### 10.2.5 等待状态处理

**当前状态: `DELAYED` / `PENDING_VERSION` / `WAITING_FOR_DEPLOY`

**处理流程：** ACK 后重新调度

**代码实现** (`apps/webapp/app/v3/taskRunHeartbeatFailed.server.ts:90-107
```typescript
case "DELAYED":
case "PENDING_VERSION":
case "WAITING_FOR_DEPLOY":
  // 这些状态由专门的系统处理
  // ACK 确认，不重试
  await marqs?.acknowledgeMessage(taskRun.id);
  break;
```

**各状态对应的重新调度机制：

| 状态 | 重新调度触发点 |
|------|-------------|
| `DELAYED` | 延迟时间到达后自动重新入队 |
| `PENDING_VERSION` | 新版本部署完成后由 `PendingVersionSystem` 处理 |
| `WAITING_FOR_DEPLOY` | 部署完成后由 `ExecuteTasksWaitingForDeployService` 处理 |

#### 10.2.6 终态处理

**当前状态:** `COMPLETED_SUCCESSFULLY` / `CANCELED` / `SYSTEM_FAILURE` 等

**处理流程：** ACK 忽略

**说明：**
- 任务已完成，不需要任何处理
- 发送取消信号给残留容器（如果有的话）

---

### 10.3 锁机制在任务接管中的作用

#### 10.3.1 分布式锁（RunLocker）

**核心作用：防止并发修改同一任务

**代码实现** (`internal-packages/run-engine/src/engine/locking.ts:70-145
```typescript
export class RunLocker {
  private redlock: InstanceType<typeof redlock.default>;

  async lock<T>(
    name: string,
    resources: string[],
    callback: (signal: redlock.RedlockAbortSignal) => Promise<T>,
    options?: LockRetryConfig
  ): Promise<T> {
    // 基于 Redlock 算法的分布式锁
    // 锁自动延期：接近过期时自动续期
    // 锁超时：默认 5000ms，防止死锁
  }
}
```

**在 NACK 流程中：**
- NACK 操作前获取锁
- 确保同一时间只有一个进程能修改任务状态
- 防止重复 NACK 导致状态混乱

#### 10.3.2 并发控制集合（Concurrency Sets）

**作用：** 限制环境级、队列级并发数

**Redis 数据结构：
```
- {queue}:currentConcurrency  (Set) - 队列当前执行中任务
- {env}:currentConcurrency      (Set) - 环境当前执行中任务
- {queue}:currentDequeued          (Set) - 队列已出队任务
- {env}:currentDequeued          (Set) - 环境已出队任务
```

**NACK 时清理：
```lua
-- 从并发集合中移除
redis.call('SREM', queueCurrentConcurrencyKey, messageId)
redis.call('SREM', envCurrentConcurrencyKey, messageId)
redis.call('SREM', queueCurrentDequeuedKey, messageId)
redis.call('SREM', envCurrentDequeuedKey, messageId)
```

---

### 10.4 新节点重新领取任务流程

**完整接管后，任务重新出队：

```
NACK 重新入队
    ↓
消息回到 ZSET (messageQueueKey (Sorted Set)
    ↓
Master Queue Rebalance 重新平衡
    ↓
Worker Consumer 消费者轮询
    ↓
dequeueMessageFromWorkerQueue
    ↓
获取锁 → RunLocker
    ↓
DequeueSystem.dequeueFromWorkerQueue
    ↓
# 检查执行快照状态检查
    ├─ 检查版本匹配检查
    └─ 并发限制检查
    └─ 状态有效性检查
    ↓
新版本 Worker 领取成功
```

---

## 11. 滚动发布防止双重消费端到端链路详解

### 11.1 触发条件

#### 11.1.1 部署流程概览

```
用户触发部署
    ↓
CreateDeploymentBackgroundWorkerV4
    ↓
WorkerDeployment 状态: DEPLOYING
    ↓
构建镜像 (外部 CI/CD)
    ↓
镜像构建完成
    ↓
FinalizeDeploymentService.call()
    ↓
WorkerDeployment 状态: DEPLOYED
    ↓
ChangeCurrentDeploymentService.call("promote")
    ↓
设置为 CURRENT_DEPLOYMENT
    ↓
PendingVersionSystem.scheduleResolvePendingVersionRuns
    ↓
批量处理 PENDING_VERSION 任务
```

#### 11.1.2 触发时机

**触发条件：
1. **新任务入队时机**：没有匹配的 worker 版本
2. **部署完成时机**：新版本部署完成后
3. **版本切换时机**：`ChangeCurrentDeploymentService

---

### 11.2 关键数据结构

#### 11.2.1 核心数据表

**BackgroundWorker 表字段：
```typescript
{
  id: string,              // Worker ID
  version: string,         // 版本号 (如 "v1.0.0")
  runtimeEnvironmentId: string,  // 环境 ID
  deploymentId: string,        // 部署 ID
  status: "ACTIVE" | "INACTIVE",
  tasks: BackgroundWorkerTask[],  // 任务列表
  queues: TaskQueue[],     // 队列列表
}
```

**WorkerDeployment 表字段：
```typescript
{
  id: string,
  friendlyId: string,      // 部署友好 ID
  version: string,         // 部署版本
  status: "DEPLOYING" | "DEPLOYED" | "FAILED",
  workerId: string,       // 关联的 Worker ID
  imageReference: string,  // 镜像引用
  shortCode: string,     // 短码
}
```

**WorkerDeploymentPromotion 表：
```typescript
{
  environmentId: string,
  label: "CURRENT_DEPLOYMENT",
  deploymentId: string,  // 当前部署版本
}
```

**TaskRun 表关键字段：
```typescript
{
  status: TaskRunStatus,
  lockedToVersionId: string | null,  // 锁定到的版本 ID
  lockedQueueId: string | null,    // 锁定到的队列 ID
  taskIdentifier: string,          // 任务标识符
  queue: string,            // 队列名称
}
```

#### 11.2.2 Redis 计数器 TTL 机制

**作用**：防止 v1/v2 版本重叠时的漂移

**代码说明** (`internal-packages/run-engine/src/run-queue/index.ts:84-86
```typescript
/**
 * TTL (seconds) applied to the per-base-queue lengthCounter/runningCounter on
 * lazy-init. Bounds the maximum window for any drift accumulated during a
 * rolling-deploy v1/v2 overlap.
 */
counterTtlSeconds?: number;  // 默认: 86400 (24h)
```

**设计原理**：
- 每个 base-queue 的 lengthCounter/runningCounter 计数器有 TTL
- INCR/DECR 操作不会延长 TTL
- 计数器过期后，下一次 CK 操作会从 ckIndex 重新锚定
- 滚动部署 v1/v2 重叠期间的漂移被限制在 24h 内

---

### 11.3 版本约束验证链路

#### 11.3.1 出队时版本验证

**代码实现** (`internal-packages/run-engine/src/engine/systems/dequeueSystem.ts:809-964
```typescript
async #getRunWithBackgroundWorkerTasks(
  prisma,
  runId,
  backgroundWorkerId
): Promise<RunWithBackgroundWorkerTasksResult> {
  // 1. 读取 TaskRun
  const run = await prisma.taskRun.findFirst({
    where: { id: runId },
    include: {
      runtimeEnvironment: true,
      lockedToVersion: { include: { deployment: true, tasks: true }
    }
  });

  // 2. 确定 Worker ID
  const workerId = run.lockedToVersionId ?? backgroundWorkerId;

  // 3. 获取 Worker 信息（根据环境类型选择不同策略：
  // - DEVELOPMENT: 最新版本
  // - 生产环境: 当前 promoted 版本
  let workerWithTasks: WorkerDeploymentWithWorkerTasks | null = null;

  if (run.runtimeEnvironment.type === "DEVELOPMENT") {
    workerWithTasks = workerId
      ? await this.#getWorkerById(prisma, workerId)
      : await this.#getMostRecentWorker(prisma, run.runtimeEnvironmentId);
  } else {
    workerWithTasks = workerId
      ? await this.#getWorkerDeploymentFromWorker(prisma, workerId)
      : await this.#getManagedWorkerFromCurrentlyPromotedDeployment(
          prisma,
          run.runtimeEnvironmentId
        );
  }

  // 4. Worker 存在性检查
  if (!workerWithTasks) {
    return { success: false, code: "NO_WORKER", message: ... }
  }

  // 5. Worker 匹配检查
  if (backgroundWorkerId !== workerWithTasks.worker.id) {
    return { success: false, code: "BACKGROUND_WORKER_MISMATCH", message: ... }
  }

  // 6. 任务存在性检查
  const backgroundTask = workerWithTasks.tasks.find(
    task => task.slug === run.taskIdentifier
  );
  if (!backgroundTask) {
    // 检查任务是否曾经存在过
    const nonCurrentTask = await prisma.backgroundWorkerTask.findFirst({...});
    if (nonCurrentTask) {
      return { success: false, code: "TASK_NOT_IN_LATEST", message: ... }
    } else {
      return { success: false, code: "TASK_NEVER_REGISTERED", message: ... }
    }
  }

  // 7. 队列存在性检查
  const queue = workerWithTasks.queues.find(
    queue => run.lockedQueueId
      ? queue.id === run.lockedQueueId
      : queue.name === run.queue
  );
  if (!queue) {
    return { success: false, code: "QUEUE_NOT_FOUND", message: ... }
  }

  return { success: true, run, worker, task, queue, deployment };
}
```

#### 11.3.2 版本锁定机制

**出队时锁定版本** (`internal-packages/run-engine/src/engine/systems/dequeueSystem.ts:422-450
```typescript
// 任务出队时，锁定到特定版本和队列
const lockedTaskRun = await prisma.taskRun.update({
  where: { id: runId },
  data: {
    lockedAt,
    lockedById: result.task.id,
    lockedToVersionId: result.worker.id,  // 锁定版本
    lockedQueueId: result.queue.id,    // 锁定队列
    status: "DEQUEUED",
    startedAt,
    taskVersion: result.worker.version,  // 记录任务版本
    sdkVersion: result.worker.sdkVersion,
    cliVersion: result.worker.cliVersion,
    // ... 更多锁定配置
  }
});
```

**锁定后的影响：**
- 后续重试时，会使用 lockedToVersionId 来查找 Worker
- 确保任务始终在同一版本的 Worker 上执行
- 防止版本切换导致的版本不匹配

---

### 11.4 PENDING_VERSION 状态处理链路

#### 11.4.1 入队时设置 PENDING_VERSION

**触发时机**：新任务入队时，如果没有匹配的 Worker 版本

**代码实现** (`internal-packages/run-engine/src/engine/systems/enqueueSystem.ts
```typescript
async enqueueRun(...) {
  // 检查是否有匹配的 Worker 版本
  const hasMatchingWorkerVersion = await this.#checkWorkerVersion(...);

  if (!hasMatchingWorkerVersion) {
    // 设置为 PENDING_VERSION 状态
    await tx.taskRun.update({
      where: { id: run.id },
      data: { status: "PENDING_VERSION" },
    });
    return;
  }

  // 正常入队
  await this.#enqueueToRunQueue(...);
}
```

#### 11.4.2 部署完成后处理

**触发** (`internal-packages/run-engine/src/engine/systems/pendingVersionSystem.ts:19-133
```typescript
async enqueueRunsForBackgroundWorker(backgroundWorkerId: string) {
  const maxCount = this.options.queueRunsPendingVersionBatchSize ?? 200;

  // 1. 查询 PENDING_VERSION 的任务
  const pendingRuns = await this.$.readOnlyPrisma.taskRun.findMany({
    where: {
      runtimeEnvironmentId: backgroundWorker.runtimeEnvironmentId,
      projectId: backgroundWorker.projectId,
      status: "PENDING_VERSION",
      taskIdentifier: { in: backgroundWorker.tasks.map(t => t.slug) },
      queue: { in: backgroundWorker.queues.map(q => q.name) },
    },
    orderBy: { createdAt: "asc" },
    take: maxCount + 1,
  });

  // 2. 批量处理
  for (const run of pendingRuns.slice(0, maxCount)) {
    await this.$.prisma.$transaction(async (tx) => {
      // 更新状态为 PENDING
      const updatedRun = await tx.taskRun.update({
        where: { id: run.id },
        data: { status: "PENDING" },
      });
      // 正常入队
      await this.enqueueSystem.enqueueRun({ run: updatedRun, ... });
    });
  }

  // 3. 还有更多，继续调度
  if (pendingRuns.length > maxCount) {
    await this.scheduleResolvePendingVersionRuns(backgroundWorkerId);
  }
}
```

---

### 11.5 部署切换链路

#### 11.5.1 FinalizeDeployment 流程

**代码实现** (`apps/webapp/app/v3/services/finalizeDeployment.server.ts:16-151
```typescript
export class FinalizeDeploymentService extends BaseService {
  public async call(authenticatedEnv, id, body) {
    // 1. 验证部署状态
    if (deployment.status !== "DEPLOYING") {
      throw new ServiceValidationError(...);
    }

    // 2. 更新部署状态为 DEPLOYED
    const finalizedDeployment = await this._prisma.workerDeployment.update({
      where: { id: deployment.id },
      data: {
        status: "DEPLOYED",
        deployedAt: new Date(),
        imageReference: imageDigest ? `${deployment.imageReference}@${imageDigest}` : undefined,
      },
    });

    // 3. 提升为当前部署（除非 skipPromotion）
    if (typeof body.skipPromotion === "undefined" || !body.skipPromotion) {
      const promotionService = new ChangeCurrentDeploymentService();
      await promotionService.call(finalizedDeployment, "promote");
    }

    // 4. 处理 PENDING_VERSION 任务
    if (deployment.worker.engine === "V2") {
      await engine.scheduleEnqueueRunsForBackgroundWorker(deployment.worker.id);
    }

    return finalizedDeployment;
  }
}
```

#### 11.5.2 ChangeCurrentDeployment 流程

**代码实现** (`apps/webapp/app/v3/services/changeCurrentDeployment.server.ts:30-179
```typescript
export class ChangeCurrentDeploymentService extends BaseService {
  public async call(deployment, direction) {
    // 1. 版本验证
    if (currentPromotion) {
      // 检查版本方向
      switch (direction) {
        case "promote": {
          if (compareDeploymentVersions(
            currentPromotion.deployment.version,
            deployment.version
          ) >= 0) {
            throw new ServiceValidationError(
              "Cannot promote a deployment that is older than the current deployment."
            );
          }
          break;
        }
        case "rollback": {
          if (compareDeploymentVersions(
            currentPromotion.deployment.version,
            deployment.version
          ) <= 0) {
            throw new ServiceValidationError(
              "Cannot rollback to a deployment that is newer than the current deployment."
            );
          }
          break;
        }
      }
    }

    // 2. 设置为当前部署
    await this._prisma.workerDeploymentPromotion.upsert({
      where: {
        environmentId_label: {
          environmentId: deployment.environmentId,
          label: CURRENT_DEPLOYMENT_LABEL,
        },
      },
      create: {
        deploymentId: deployment.id,
        environmentId: deployment.environmentId,
        label: CURRENT_DEPLOYMENT_LABEL,
      },
      update: {
        deploymentId: deployment.id,
      },
    });

    // 3. 同步任务标识符
    await syncTaskIdentifiers(...);

    // 4. 刷新任务元数据缓存
    await this._taskMetaCache.populateByCurrentWorker(...);

    // 5. 同步调度
    await this.#syncSchedulesForDeployment(deployment);

    // 6. 执行等待部署的任务
    await ExecuteTasksWaitingForDeployService.enqueue(deployment.workerId);
  }
}
```

---

### 11.6 防止双重消费的保障机制

#### 11.6.1 五层防护机制

| 防护层级 | 机制 | 作用 |
|---------|------|------|
| **第一层** | PENDING_VERSION 状态 | 新版本部署前不进入执行队列 |
| **第二层** | lockedToVersion 锁定 | 出队时锁定到特定版本 |
| **第三层** | 版本验证 | 出队时验证 Worker 版本匹配 |
| **第四层** | 计数器 TTL | 限制 v1/v2 重叠漂移 |
| **第五层** | 分布式锁 | 防止并发修改 |

#### 11.6.2 版本不匹配时的处理

**出队时版本不匹配的处理：

```typescript
// dequeueSystem.ts:287-350
switch (result.code) {
  case "NO_WORKER":
  case "TASK_NEVER_REGISTERED":
  case "QUEUE_NOT_FOUND":
  case "TASK_NOT_IN_LATEST": {
    // 任务还没部署，进入 PENDING_VERSION 等待
    await this.#pendingVersion({
      orgId, runId, reason: result.message, statusReason: result.code, tx: prisma,
    });
    return;
  }
  case "BACKGROUND_WORKER_MISMATCH": {
    // Worker 不匹配，NACK 重新入队
    await this.$.runQueue.nackMessage({ orgId, messageId: runId });
    return;
  }
}
```

---

## 12. 状态转换矩阵与锁作用总结

### 12.1 节点掉线状态转换矩阵

| 当前状态 | 处理方式 | 新状态 | 原因 |
|---------|----------|---------|------|
| **PENDING** | NACK 重新入队 | PENDING | 任务未执行，可安全重试 |
| **DEQUEUED** | NACK 重新入队 | PENDING | 已领取未执行，可安全重试 |
| **WAITING_TO_RESUME** | NACK 重新入队 | PENDING | 等待中，可安全重试 |
| **PAUSED** | NACK 重新入队 | PENDING | 暂停中，可安全重试 |
| **EXECUTING** | 标记失败 | SYSTEM_FAILURE | 执行中掉线，风险高 |
| **RETRYING_AFTER_FAILURE** | 标记失败 | SYSTEM_FAILURE | 重试中掉线，风险高 |
| **DELAYED** | ACK 后重新调度 | DELAYED | 延迟中，由延迟系统处理 |
| **PENDING_VERSION** | ACK 后重新调度 | PENDING_VERSION | 等待版本，由版本系统处理 |
| **WAITING_FOR_DEPLOY** | ACK 后重新调度 | WAITING_FOR_DEPLOY | 等待部署，由部署系统处理 |
| **终态** | ACK 忽略 | 不变 | 任务已完成 |

### 12.2 滚动发布状态转换矩阵

| 触发事件 | 处理方式 | 新状态 | 原因 |
|---------|----------|---------|------|
| **新任务入队，无匹配 Worker** | 设置 PENDING_VERSION | PENDING_VERSION | 等待新版本部署 |
| **部署完成** | 批量处理 PENDING_VERSION | PENDING | 新版本就绪，进入队列 |
| **出队时版本不匹配** | 重新设置 PENDING_VERSION | PENDING_VERSION | 任务不在最新版本 |
| **出队时 Worker 不匹配** | NACK 重新入队 | PENDING | Worker 版本不匹配 |
| **出队成功** | 锁定版本 | DEQUEUED | 锁定到特定版本 |

### 12.3 锁的作用总结

| 锁类型 | 作用场景 | 保护目标 |
|---------|----------|----------|
| **RunLocker 分布式锁 | 出队/NACK/ACK | 防止并发修改同一任务 |
| **并发集合锁** | 出队/NACK | 环境/队列并发控制 |
| **版本锁定 (lockedToVersionId) | 出队后 | 确保任务始终在同一版本执行 |
| **队列锁定 (lockedQueueId) | 出队后 | 确保任务始终在同一队列执行 |
| **计数器 TTL | 滚动发布期间 | 限制 v1/v2 重叠漂移 |

---

## 13. Socket 断开与心跳超时双路径时序分析

### 13.1 核心发现：Socket 断开不直接触发任务接管

经过代码追踪，**Socket 断开事件本身并不直接触发任务状态变更或接管流程**。Socket 断开仅做清理工作（取消事件监听、停止定时器），真正的任务接管完全依赖心跳超时路径。

这一设计是刻意的：Socket 断开可能是瞬时网络抖动导致的短暂重连，不应立即判定节点死亡。心跳超时提供了更可靠的检测窗口。

### 13.2 两条路径的完整代码追踪

#### 路径 A：Socket 断开事件

```
┌─────────────────────────────────────────────────────────────────────┐
│                       Socket 断开事件链路                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 连接断开 (网络故障/进程退出/节点下线)                              │
│     │                                                               │
│     ▼                                                               │
│  2. Socket.IO 服务端触发 "disconnect" 事件                           │
│     │                                                               │
│     ├── coordinator 命名空间断开:                                    │
│     │   apps/coordinator/src/index.ts:1459-1468                     │
│     │   → onDisconnect: 仅发送 LOG 消息到 platform                   │
│     │   → 无任何任务状态变更                                          │
│     │                                                               │
│     ├── worker 命名空间断开:                                         │
│     │   apps/webapp/app/v3/handleSocketIo.server.ts:550-560         │
│     │   → 清理 interval 定时器                                       │
│     │   → 取消 eventBus "workerNotification" 监听                    │
│     │   → 无任何任务状态变更                                          │
│     │                                                               │
│     └── shared-queue 命名空间断开:                                   │
│         apps/webapp/app/v3/sharedSocketConnection.ts:136-139        │
│         → 停止 SharedQueueConsumerPool                               │
│         → 触发 onClose 事件 (仅日志记录)                              │
│         → 无任何任务状态变更                                          │
│                                                                     │
│  3. 后果：心跳停止发送 → 进入路径 B                                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**关键代码** — `handleSocketIo.server.ts:550-560`：
```typescript
socket.on("disconnect", (reason, description) => {
  logger.debug("worker disconnected", {
    namespace, socketId: socket.id, reason, description,
  });
  clearInterval(interval);
  engine.eventBus.off("workerNotification", onNotification);
  // ⚠️ 没有任何任务状态变更操作
});
```

**关键代码** — `coordinator/src/index.ts:1459-1468`：
```typescript
onDisconnect: async (socket, handler, sender, logger) => {
  try {
    this.#platformSocket?.send("LOG", {
      metadata: socket.data,
      text: "disconnect",
    });
  } catch (error) {
    logger.error("onDisconnect error", { error });
  }
  // ⚠️ 仅记录日志，无任务状态变更
},
```

#### 路径 B：心跳超时

```
┌─────────────────────────────────────────────────────────────────────┐
│                      心跳超时事件链路                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. 正常心跳流（持续重置超时）                                        │
│     │                                                               │
│     ├── V1 引擎 (MarQS):                                            │
│     │   Worker → Socket "TASK_RUN_HEARTBEAT" →                      │
│     │   handleSocketIo.server.ts:223 →                               │
│     │   sharedQueueTasks.taskRunHeartbeat() →                        │
│     │   marqs.heartbeatMessage() →                                   │
│     │   V3GraphileVisibilityTimeout.heartbeat() →                    │
│     │   TaskRunHeartbeatFailedService.enqueue(新超时时间)             │
│     │                                                               │
│     └── V2 引擎 (RunQueue):                                         │
│         Worker → POST /runs/{id}/snapshots/{sid}/heartbeat →        │
│         SupervisorSession →                                          │
│         runEngineHandlers → marqs.heartbeatMessage() →              │
│         V3GraphileVisibilityTimeout.heartbeat() →                    │
│         TaskRunHeartbeatFailedService.enqueue(新超时时间)             │
│                                                                     │
│  2. 心跳超时（到期未重置）                                            │
│     │                                                               │
│     ▼                                                               │
│  3. Graphile Worker 执行 TaskRunHeartbeatFailedService.call()        │
│     apps/webapp/app/v3/taskRunHeartbeatFailed.server.ts:13-156      │
│     │                                                               │
│     ├── 查询任务当前状态                                              │
│     │                                                               │
│     ├── PENDING/DEQUEUED/WAITING_TO_RESUME/PAUSED:                  │
│     │   → marqs.nackMessage() → 重新入队                             │
│     │   → 如果 nack 失败 → FailedTaskRunService (标记失败)           │
│     │                                                               │
│     ├── EXECUTING/RETRYING_AFTER_FAILURE:                           │
│     │   → FailedTaskRunService (直接标记失败)                         │
│     │                                                               │
│     ├── DELAYED/PENDING_VERSION/WAITING_FOR_DEPLOY:                 │
│     │   → marqs.acknowledgeMessage() → 由专门系统重新调度            │
│     │                                                               │
│     └── 终态 (COMPLETED/CANCELED/SYSTEM_FAILURE...):                │
│         → marqs.acknowledgeMessage() → 忽略                         │
│         → 发送 REQUEST_RUN_CANCELLATION 到残留容器                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 13.3 双路径汇聚分析

#### 13.3.1 汇聚点

**Socket 断开与心跳超时的唯一汇聚点是心跳超时触发后的 `TaskRunHeartbeatFailedService.call()`**。

```
Socket 断开                    心跳超时
     │                            │
     │ (仅清理监听器/定时器)        │ (实际触发任务状态变更)
     │                            │
     ▼                            ▼
  心跳停止发送 ──────────────→ 心跳超时到期
                                    │
                                    ▼
                          TaskRunHeartbeatFailedService.call()
                          (apps/webapp/app/v3/taskRunHeartbeatFailed.server.ts:13)
                                    │
                                    ▼
                              按状态分支处理
```

Socket 断开只是**间接**导致心跳停止发送，真正的接管决策完全由心跳超时机制驱动。

#### 13.3.2 时序关系

```
时间轴：
T0: 节点故障/网络断开
    │
    ├── [立即] Socket.IO 检测到连接断开
    │   → "disconnect" 事件触发
    │   → 清理 Socket 监听器、停止 SharedQueueConsumerPool
    │   → ⚠️ 无任务状态变更
    │
    ├── [T0 → T0+visibilityTimeoutInMs] 心跳停止但超时未到期
    │   → V3GraphileVisibilityTimeout 正在等待
    │   → 任务仍在原节点"持有"状态
    │   → 此窗口期内的任务无法被新节点接管
    │
    └── [T0+visibilityTimeoutInMs] 心跳超时到期
        → Graphile Worker 执行 TaskRunHeartbeatFailedService.call()
        → 根据任务状态执行 NACK/ACK/失败标记
        → 新节点可开始接管
```

**默认超时配置** (`apps/webapp/app/env.server.ts:602-605`)：
```typescript
MARQS_VISIBILITY_TIMEOUT_MS: z.coerce.number().int().default(60 * 1000 * 15),
// 默认: 900,000ms = 15 分钟
```

**这意味着从节点故障到新节点接管，最坏情况下需等待 15 分钟。**

### 13.4 V1 与 V2 引擎的心跳路径对比

| 特性 | V1 引擎 (MarQS) | V2 引擎 (RunQueue) |
|------|-----------------|-------------------|
| **任务队列** | MarQS (SharedQueueConsumer) | RunQueue (DequeueSystem) |
| **心跳发送** | Socket `TASK_RUN_HEARTBEAT` 事件 | HTTP POST `/snapshots/{id}/heartbeat` |
| **心跳中转** | Coordinator → Platform (Socket) | SupervisorSession → Platform (HTTP) |
| **超时调度** | Graphile Worker (v3.requeueTaskRun) | Graphile Worker (v3.requeueTaskRun) |
| **超时处理** | `TaskRunHeartbeatFailedService` | `TaskRunHeartbeatFailedService` |
| **NACK 实现** | `marqs.nackMessage()` | `runQueue.nackMessage()` |
| **ACK 实现** | `marqs.acknowledgeMessage()` | `runQueue.acknowledgeMessage()` |
| **汇聚点** | 相同：`TaskRunHeartbeatFailedService.call()` | 相同 |

### 13.5 额外路径：RUN_CRASHED 事件

除了 Socket 断开和心跳超时外，还存在一条**主动崩溃上报路径**：

```
Worker 进程异常退出
    │
    ├── Pod 内 Coordinator 检测到异常
    │   apps/coordinator/src/index.ts:453-465
    │   → crashRun() → 发送 "RUN_CRASHED" 到 Platform
    │
    └── Provider 命名空间接收 RUN_CRASHED
        apps/webapp/app/v3/handleSocketIo.server.ts:392-404
        → CrashTaskRunService.call()
        → 判断 isCrashableRunStatus
        → FailedTaskRunRetryHelper → 尝试重试
        → 重试失败 → FinalizeTaskRunService (标记 CRASHED)
```

**关键代码** — `crashTaskRun.server.ts:23-120`：
```typescript
export class CrashTaskRunService extends BaseService {
  public async call(runId: string, options?: CrashTaskRunServiceOptions) {
    // 1. 检查任务是否在可崩溃状态
    if (!isCrashableRunStatus(taskRun.status)) return;

    // 2. 尝试重试
    const retryResult = await retryHelper.call({
      runId,
      completion: { ok: false, error: { ... } },
      isCrash: true,
    });

    // 3. 重试成功则直接返回
    if (retryResult === "RETRIED") return;

    // 4. 重试失败，标记为 CRASHED
    await finalizeService.call({ status: "CRASHED", ... });
  }
}
```

**与心跳超时的区别：**
- `RUN_CRASHED` 是**主动上报**，延迟极低
- 心跳超时是**被动检测**，需等待 visibilityTimeout 窗口
- `RUN_CRASHED` 走 `CrashTaskRunService`，有重试逻辑
- 心跳超时走 `TaskRunHeartbeatFailedService`，按状态分支处理

### 13.6 三条路径完整对比

| 特性 | Socket 断开 | 心跳超时 | RUN_CRASHED |
|------|-----------|---------|-------------|
| **触发源** | 网络断开/节点下线 | visibilityTimeout 到期 | Worker 进程异常 |
| **检测延迟** | 秒级 | 15 分钟（默认） | 秒级 |
| **触发任务状态变更** | ❌ 否 | ✅ 是 | ✅ 是 |
| **处理服务** | 无 | `TaskRunHeartbeatFailedService` | `CrashTaskRunService` |
| **能否触发重试** | ❌ 间接 | ✅ 按状态 | ✅ 有重试逻辑 |
| **最终汇聚点** | → 心跳超时 | `marqs.nackMessage()` / `FailedTaskRunService` | `FinalizeTaskRunService` |
| **适用场景** | 所有断连 | 所有无心跳场景 | 仅 Pod 内可检测的崩溃 |

### 13.7 新节点接管任务的完整时序

```
T0: 节点 A 故障
│
├── [T0+0s] Socket.IO "disconnect" 事件
│   → 清理 Socket 资源
│   → ⚠️ 任务状态不变，仍在节点 A "持有"
│
├── [T0 → T0+15min] 心跳超时窗口
│   │
│   ├── 任务心跳到期触发:
│   │   TaskRunHeartbeatFailedService.call()
│   │   │
│   │   ├── 状态 PENDING/DEQUEUED/WAITING_TO_RESUME/PAUSED:
│   │   │   → marqs.nackMessage(taskRun.id)
│   │   │   → 释放并发集合 (Lua 原子操作)
│   │   │   → 消息回到 ZSET 队列
│   │   │   → 调度 processQueueForWorkerQueue (500ms 去重延迟)
│   │   │
│   │   ├── 状态 EXECUTING/RETRYING_AFTER_FAILURE:
│   │   │   → FailedTaskRunService.call()
│   │   │   → 标记为 SYSTEM_FAILURE
│   │   │   → 如果有重试次数，可能重新入队
│   │   │
│   │   └── 状态 DELAYED/PENDING_VERSION/WAITING_FOR_DEPLOY:
│   │       → marqs.acknowledgeMessage()
│   │       → 由专门系统稍后重新调度
│   │
│   └── [同时] 如果是 Pod 崩溃且 Coordinator 存活:
│       → RUN_CRASHED 事件 → CrashTaskRunService
│       → 尝试重试 → 可能更快恢复
│
├── [T0+15min+] 新节点 B 可领取任务
│   │
│   ├── V1 引擎: SharedQueueConsumer 从 SharedWorkerQueue 出队
│   │   → dequeueMessageFromSharedWorkerQueue()
│   │   → #handleExecuteMessage()
│   │   → 查找匹配的 deployment/worker
│   │   → 创建 TaskRunAttempt
│   │   → 发送执行载荷给 Worker
│   │
│   └── V2 引擎: DequeueSystem 从 WorkerQueue 出队
│       → dequeueFromWorkerQueue()
│       → RunLocker.lock() 获取分布式锁
│       → #getRunWithBackgroundWorkerTasks() 版本验证
│       → 更新 lockedToVersionId, status="DEQUEUED"
│       → 创建 ExecutionSnapshot (PENDING_EXECUTING)
│       → 通知 Worker 执行
│
└── [T0+15min++] 任务在新节点 B 上重新执行
    → 新的心跳开始
    → 新的 VisibilityTimeout 开始计时
```

### 13.8 关键函数标注

| 函数 | 文件位置 | 作用 |
|------|---------|------|
| `handleSocketIo.server.ts#L550-560` | `apps/webapp/app/v3/` | Socket 断开事件处理（仅清理） |
| `coordinator/index.ts#L1459-1468` | `apps/coordinator/src/` | Coordinator Socket 断开（仅日志） |
| `sharedSocketConnection.ts#L136-139` | `apps/webapp/app/v3/` | SharedQueue Socket 断开（停 ConsumerPool） |
| `V3GraphileVisibilityTimeout.startHeartbeat()` | `apps/webapp/app/v3/marqs/` | 启动心跳超时计时器 |
| `V3GraphileVisibilityTimeout.heartbeat()` | `apps/webapp/app/v3/marqs/` | 重置心跳超时（重新入队超时任务） |
| `TaskRunHeartbeatFailedService.call()` | `apps/webapp/app/v3/` | **核心汇聚点**：心跳超时后按状态分支处理 |
| `TaskRunHeartbeatFailedService.enqueue()` | `apps/webapp/app/v3/` | 调度心跳超时检测任务 |
| `marqs.nackMessage()` | `apps/webapp/app/v3/marqs/` | V1 引擎 NACK 重新入队 |
| `marqs.acknowledgeMessage()` | `apps/webapp/app/v3/marqs/` | V1 引擎 ACK 确认 |
| `FailedTaskRunService.call()` | `apps/webapp/app/v3/` | 标记任务失败（不可重试） |
| `CrashTaskRunService.call()` | `apps/webapp/app/v3/services/` | 主动崩溃上报处理（有重试） |
| `FailedTaskRunRetryHelper.call()` | `apps/webapp/app/v3/` | 失败任务重试决策 |
| `FinalizeTaskRunService.call()` | `apps/webapp/app/v3/services/` | 终态确认（CRASHED/SYSTEM_FAILURE） |
| `runQueue.nackMessage()` | `internal-packages/run-engine/src/run-queue/` | V2 引擎 NACK（Lua 原子操作） |
| `DequeueSystem.dequeueFromWorkerQueue()` | `internal-packages/run-engine/src/engine/systems/` | V2 引擎出队（含版本验证+锁） |
| `coordinator/index.ts#L453-465` | `apps/coordinator/src/` | crashRun() 主动上报崩溃 |
| `handleSocketIo.server.ts#L392-404` | `apps/webapp/app/v3/` | Provider 接收 RUN_CRASHED |

### 13.9 设计哲学总结

1. **Socket 断开仅做资源清理，不做业务决策** — 避免网络抖动导致的误判
2. **心跳超时是唯一可靠的故障检测器** — 通过超时窗口容忍短暂断连
3. **默认 15 分钟超时窗口是权衡值** — 足够长以容忍瞬时故障，足够短以避免任务长期悬挂
4. **RUN_CRASHED 作为快速路径补充** — 在 Coordinator 存活时可秒级感知崩溃
5. **所有路径最终汇聚于状态判断** — `TaskRunHeartbeatFailedService` 是唯一的任务接管决策点

---

## 6. 关键数据结构与流程总结

### 6.1 核心数据表

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `WorkerInstanceGroup` | 工作机组 | `type`(MANAGED/UNMANAGED), `masterQueue`, `tokenId` |
| `BackgroundWorker` | Worker 注册 | `version`, `deploymentId`, `runtimeEnvironmentId` |
| `WorkerDeployment` | 部署版本 | `friendlyId`, `version`, `status` |
| `TaskRun` | 任务运行 | `status`, `lockedToVersionId`, `attemptNumber` |
| `TaskRunExecutionSnapshot` | 执行快照 | `executionStatus`, `checkpointId` |

### 6.2 状态流转

**任务状态机：**
```
PENDING_VERSION
     ↓ (版本就绪)
PENDING
     ↓ (出队)
DEQUEUED
     ↓ (开始执行)
EXECUTING
     ↓ (成功/失败)
COMPLETED_SUCCESSFULLY / COMPLETED_WITH_ERRORS / SYSTEM_FAILURE
```

**心跳超时处理分支：**
```
心跳超时
   │
   ├─ 状态 ∈ {PENDING, DEQUEUED, WAITING_TO_RESUME, PAUSED} → NACK → 重新入队
   │
   ├─ 状态 ∈ {EXECUTING, RETRYING_AFTER_FAILURE} → 标记失败
   │
   └─ 状态 ∈ {DELAYED, PENDING_VERSION, WAITING_FOR_DEPLOY} → ACK → 重新调度
```

### 6.3 关键 API 端点

| 端点 | 方法 | 用途 |
|------|------|------|
| `/engine/v1/worker-actions/connect` | POST | 节点注册连接 |
| `/engine/v1/worker-actions/heartbeat` | POST | 节点心跳 |
| `/engine/v1/worker-actions/dequeue` | POST | 领取任务 |
| `/engine/v1/worker-actions/runs/{runId}/snapshots/{snapshotId}/heartbeat` | POST | 任务心跳 |
| `/engine/v1/worker-actions/runs/{runId}/snapshots/{snapshotId}/attempts/start` | POST | 开始执行 |
| `/engine/v1/worker-actions/runs/{runId}/snapshots/{snapshotId}/attempts/complete` | POST | 完成执行 |

---

## 7. 核心设计亮点

### 7.1 可靠性设计

1. **双层心跳** - 节点心跳 + 任务心跳，不同粒度的超时处理
2. **VisibilityTimeout** - 任务级别的超时检测，防止任务永久丢失
3. **分布式锁** - 基于 Redlock 防止并发修改同一任务
4. **死信队列** - 超过重试次数的任务进入 DLQ，避免阻塞队列
5. **幂等性设计** - `enqueueOnce`、`deduplicationKey` 等机制

### 7.2 可伸缩性设计

1. **消费者池动态伸缩** - 基于队列长度的 EWMA 平滑算法
2. **多租户公平出队** - MarQS 公平调度策略，防止单个租户饿死
3. **并发控制** - 环境级、队列级、任务级多层并发限制
4. **冷却时间** - 伸缩操作的冷却时间防止频繁抖动

### 7.3 可观测性设计

1. **OpenTelemetry 集成** - 全链路追踪
2. **Prometheus 指标** - 队列长度、消费者数量、伸缩决策等
3. **结构化日志** - 所有关键操作都有详细日志
4. **事件总线** - 状态变更通过事件总线通知相关系统

---

## 8. 文件索引

### 8.1 核心实现文件

| 模块 | 文件路径 | 主要职责 |
|------|---------|----------|
| Supervisor 入口 | `apps/supervisor/src/index.ts` | 编排平台主入口，运行时选择 |
| 会话管理 | `packages/core/src/v3/runEngineWorker/supervisor/session.ts` | 节点注册、心跳、任务分发 |
| 消费者池 | `packages/core/src/v3/runEngineWorker/supervisor/consumerPool.ts` | 动态伸缩、消费者管理 |
| 队列消费者 | `packages/core/src/v3/runEngineWorker/supervisor/queueConsumer.ts` | 任务轮询领取 |
| HTTP 客户端 | `packages/core/src/v3/runEngineWorker/supervisor/http.ts` | Worker API 客户端 |
| 心跳管理器 | `packages/core/src/v3/heartbeats/manager.ts` | 通用心跳机制 |
| Redis Worker | `packages/redis-worker/src/worker.ts` | 通用 Worker 基类 |
| Docker 编排 | `apps/supervisor/src/workloadManager/docker.ts` | Docker 容器管理 |
| K8s 编排 | `apps/supervisor/src/workloadManager/kubernetes.ts` | Kubernetes Pod 管理 |
| Compute 编排 | `apps/supervisor/src/workloadManager/compute.ts` | 外部计算网关 |
| Pod 清理 | `apps/supervisor/src/services/podCleaner.ts` | 已完成 Pod 清理 |
| 失败 Pod 处理 | `apps/supervisor/src/services/failedPodHandler.ts` | 失败 Pod 监控与处理 |
| 出队系统 | `internal-packages/run-engine/src/engine/systems/dequeueSystem.ts` | 服务端出队逻辑 |
| 分布式锁 | `internal-packages/run-engine/src/engine/locking.ts` | 基于 Redlock 的分布式锁 |
| 运行队列 | `internal-packages/run-engine/src/run-queue/index.ts` | 任务队列核心实现 |
| 待版本系统 | `internal-packages/run-engine/src/engine/systems/pendingVersionSystem.ts` | PENDING_VERSION 处理 |
| 心跳超时处理 | `apps/webapp/app/v3/taskRunHeartbeatFailed.server.ts` | 心跳失败后的任务处理 |
| 可见性超时 | `apps/webapp/app/v3/marqs/v3VisibilityTimeout.server.ts` | 心跳超时调度策略 |
| MarQS 队列 | `apps/webapp/app/v3/marqs/index.server.ts` | 多租户可靠队列系统 |
| Worker API 路由 | `apps/webapp/app/routes/engine.v1.worker-actions.*.ts` | Worker API 端点 |

---

## 9. 总结

Trigger.dev 容器化执行节点的生命周期管理体现了以下设计原则：

1. **分层抽象** - 通过 WorkloadManager、ResourceMonitor 等接口抹平底层编排平台差异
2. **可靠性优先** - 多层心跳、分布式锁、VisibilityTimeout、死信队列等机制确保任务不丢失
3. **版本安全** - PENDING_VERSION 状态、版本锁定、并发计数器 TTL 等机制防止滚动部署时的双重消费
4. **可观测性** - 全链路追踪、指标收集、结构化日志确保系统可观测
5. **弹性伸缩** - 基于队列长度的动态伸缩算法，结合冷却时间防止抖动

整体架构既保证了在不同运行环境（Docker/Kubernetes/Compute）下的一致性体验，又通过多层次的可靠性设计确保了任务执行的正确性和稳定性。
